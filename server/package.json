{
  "name": "Boom2D Server",
  "private": true,
  "main": "src/server.ts",
  "scripts": {
    "build": "bun build src/server.ts --minify --minify-syntax --minify-whitespace --minify-identifiers --outdir=dist --outfile=server.js",
    "start": "bun dist/server.js",
    "dev": "bun --watch src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "bit-buffer": "^0.2.5"
  }
}
